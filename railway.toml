[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w packages/web"

[deploy]
startCommand = "node packages/service/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
